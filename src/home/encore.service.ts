import { Service } from "encore.dev/service";

// Encore will consider this directory and all its subdirectories as part of the "home" service.
// This service provides home/landing page functionality for the management API

export default new Service("home");